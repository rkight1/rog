{{! -*- mode: css -*- }}
body {
    font: sans-serif;
    font-size: 12pt;
    color: {{site.colors.foreground}};
    background: {{site.colors.background}};
}

a {
    color: {{site.colors.brand}};
}

h1,
h2,
h3,
h4,
h5,
h6 {
    font: serif;
}
